import { LightningElement } from 'lwc';

export default class DataImportButton extends LightningElement {

  handleClick(event) {
    //call apex method, which in turn make the callout the external api and return a update status, success or error
  }

}